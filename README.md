# Krakend Example

1. Run containers

```sh
  docker compose up -d
```

2. Send request to the following routes

```sh
  curl http://localhost:8080/time
```

```sh
  curl http://localhost:8080/users
```